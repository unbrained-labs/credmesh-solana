{
  "name": "@credmesh/solana-shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Cross-package TS constants kept in sync with Rust (programs + crates). Single source of truth for things consumed by ts/server, ts/bridge, and ts/keeper.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
