{
  "name": "credmesh-solana-keeper",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Permissionless keeper service that observes expired Advance PDAs and submits liquidate() ixs after the 14-day grace period. Mirrors the EVM lane's `scripts/keeper-liquidate.ts`.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@solana/kit": "^2.0.0",
    "bs58": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
